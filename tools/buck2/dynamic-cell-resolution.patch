diff --git a/app/buck2_file_watcher/src/edenfs/interface.rs b/app/buck2_file_watcher/src/edenfs/interface.rs
index e9d4b9d038..ab5009a156 100644
--- a/app/buck2_file_watcher/src/edenfs/interface.rs
+++ b/app/buck2_file_watcher/src/edenfs/interface.rs
@@ -11,6 +11,7 @@
 use std::path::PathBuf;
 use std::str;
 use std::sync::Arc;
+use std::sync::Mutex;

 use allocative::Allocative;
 use async_trait::async_trait;
@@ -93,7 +94,7 @@ pub(crate) struct EdenFsFileWatcher {
     eden_root: AbsNormPathBuf,
     #[allocative(skip)]
     position: RwLock<JournalPosition>,
-    cells: CellResolver,
+    cells: Mutex<CellResolver>,
     // The project root, relative to the eden mount point
     project_root: ForwardRelativePathBuf,
     ignore_specs: StdBuckHashMap<CellName, IgnoreSet>,
@@ -146,7 +147,7 @@ impl EdenFsFileWatcher {
             mount_point,
             eden_root,
             position: RwLock::new(JournalPosition::default()),
-            cells,
+            cells: Mutex::new(cells),
             project_root,
             ignore_specs,
             mergebase: RwLock::new(None),
@@ -481,14 +482,12 @@ impl EdenFsFileWatcher {
             return Ok(());
         }

-        let cell_path = self.cells.get_cell_path(project_rel_path);
+        let cells = self.cells.lock().unwrap();
+        let cell_path = cells.get_cell_path(project_rel_path);

         let ignore = self
             .ignore_specs
             .get(&cell_path.cell())
-            // This shouldn't ever really happen. However, because of the bugs caused by just
-            // storing the `CellResolver` in the watcher permanently, sometimes it can, so we just
-            // default to not ignoring the file in that case
             .is_some_and(|ignore| ignore.is_match(cell_path.path()));

         info!("EdenFS: {:?} (ignore = {})", cell_path, ignore);
@@ -824,7 +823,10 @@ impl FileWatcher for EdenFsFileWatcher {
     async fn sync(
         &self,
         dice: DiceTransactionUpdater,
+        cells: CellResolver,
     ) -> buck2_error::Result<(DiceTransactionUpdater, Mergebase)> {
+        *self.cells.lock().unwrap() = cells;
+
         span_async(
             buck2_data::FileWatcherStart {
                 provider: buck2_data::FileWatcherProvider::EdenFs as i32,
diff --git a/app/buck2_file_watcher/src/file_watcher.rs b/app/buck2_file_watcher/src/file_watcher.rs
index f119a32d0e..32be882aa6 100644
--- a/app/buck2_file_watcher/src/file_watcher.rs
+++ b/app/buck2_file_watcher/src/file_watcher.rs
@@ -39,6 +39,7 @@ pub trait FileWatcher: Allocative + Send + Sync + 'static {
     async fn sync(
         &self,
         dice: DiceTransactionUpdater,
+        cells: CellResolver,
     ) -> buck2_error::Result<(DiceTransactionUpdater, Mergebase)>;
 }

diff --git a/app/buck2_file_watcher/src/fs_hash_crawler.rs b/app/buck2_file_watcher/src/fs_hash_crawler.rs
index 82ba5b5054..ed397c645f 100644
--- a/app/buck2_file_watcher/src/fs_hash_crawler.rs
+++ b/app/buck2_file_watcher/src/fs_hash_crawler.rs
@@ -51,7 +51,7 @@ use crate::stats::FileWatcherStats;
 #[derive(Allocative)]
 pub struct FsHashCrawler {
     root: ProjectRoot,
-    cells: CellResolver,
+    cells: Mutex<CellResolver>,
     ignore_specs: StdBuckHashMap<CellName, IgnoreSet>,
     snapshot: Arc<Mutex<FsSnapshot>>,
 }
@@ -65,7 +65,7 @@ impl FsHashCrawler {
         let snapshot = Arc::new(Mutex::new(FsSnapshot::build(root, &cells)?));
         Ok(Self {
             root: root.dupe(),
-            cells,
+            cells: Mutex::new(cells),
             ignore_specs,
             snapshot,
         })
@@ -76,7 +76,7 @@ impl FsHashCrawler {
         mut dice: DiceTransactionUpdater,
     ) -> buck2_error::Result<(buck2_data::FileWatcherStats, DiceTransactionUpdater)> {
         let root = self.root.dupe();
-        let cells = self.cells.dupe();
+        let cells = self.cells.lock().unwrap().dupe();
         let new_snapshot =
             tokio::task::spawn_blocking(move || FsSnapshot::build(&root, &cells)).await??;
         let mut guard = self.snapshot.lock().unwrap();
@@ -92,7 +92,10 @@ impl FileWatcher for FsHashCrawler {
     async fn sync(
         &self,
         dice: DiceTransactionUpdater,
+        cells: CellResolver,
     ) -> buck2_error::Result<(DiceTransactionUpdater, Mergebase)> {
+        *self.cells.lock().unwrap() = cells;
+
         span_async(
             buck2_data::FileWatcherStart {
                 provider: buck2_data::FileWatcherProvider::FsHashCrawler as i32,
diff --git a/app/buck2_file_watcher/src/notify.rs b/app/buck2_file_watcher/src/notify.rs
index 9003bf5078..bf5d6a44ab 100644
--- a/app/buck2_file_watcher/src/notify.rs
+++ b/app/buck2_file_watcher/src/notify.rs
@@ -102,7 +102,6 @@ impl NotifyFileData {
             let cell_path = cells.get_cell_path(&path);
             let ignore = ignore_specs
                 .get(&cell_path.cell())
-                // See the comment on the analogous code in `watchman/interface.rs`
                 .is_some_and(|ignore| ignore.is_match(cell_path.path()));

             info!(
@@ -253,6 +252,8 @@ pub struct NotifyFileWatcher {
     // FIXME(JakobDegen): Clarify if this just needs to be kept alive or can be removed?
     watcher: RecommendedWatcher,
     data: Arc<Mutex<buck2_error::Result<NotifyFileData>>>,
+    #[allocative(skip)]
+    cells: Arc<Mutex<CellResolver>>,
 }

 impl NotifyFileWatcher {
@@ -262,11 +263,14 @@ impl NotifyFileWatcher {
         ignore_specs: StdBuckHashMap<CellName, IgnoreSet>,
     ) -> buck2_error::Result<Self> {
         let data = Arc::new(Mutex::new(Ok(NotifyFileData::new())));
+        let cells = Arc::new(Mutex::new(cells));
         let data2 = data.dupe();
         let root2 = root.dupe();
+        let cells2 = cells.dupe();
         let mut watcher = notify::recommended_watcher(move |event| {
             let mut guard = data2.lock().unwrap();
             if let Ok(state) = &mut *guard {
+                let cells = cells2.lock().unwrap();
                 if let Err(e) = state.process(event, &root2, &cells, &ignore_specs) {
                     *guard = Err(e);
                 }
@@ -276,7 +280,11 @@ impl NotifyFileWatcher {
         watcher
             .watch(root.root().as_path(), notify::RecursiveMode::Recursive)
             .map_err(|e| from_any_with_tag(e, buck2_error::ErrorTag::NotifyWatcher))?;
-        Ok(Self { watcher, data })
+        Ok(Self {
+            watcher,
+            data,
+            cells,
+        })
     }

     fn sync2(
@@ -303,7 +311,10 @@ impl FileWatcher for NotifyFileWatcher {
     async fn sync(
         &self,
         dice: DiceTransactionUpdater,
+        cells: CellResolver,
     ) -> buck2_error::Result<(DiceTransactionUpdater, Mergebase)> {
+        *self.cells.lock().unwrap() = cells;
+
         span_async(
             buck2_data::FileWatcherStart {
                 provider: buck2_data::FileWatcherProvider::RustNotify as i32,
diff --git a/app/buck2_file_watcher/src/watchman/interface.rs b/app/buck2_file_watcher/src/watchman/interface.rs
index c0cd0db2a5..74f22e8be1 100644
--- a/app/buck2_file_watcher/src/watchman/interface.rs
+++ b/app/buck2_file_watcher/src/watchman/interface.rs
@@ -9,6 +9,7 @@
  */

 use std::sync::Arc;
+use std::sync::Mutex;

 use allocative::Allocative;
 use async_trait::async_trait;
@@ -43,10 +44,7 @@ use crate::watchman::core::WatchmanKind;
 use crate::watchman::utils::find_first_valid_parent;

 struct WatchmanQueryProcessor {
-    // FIXME(JakobDegen): Storing these values statically is completely broken. See
-    // `tests/e2e/cells/test_file_watcher_resolution:test_changing_cell_location_bug` for a repro of
-    // a bug.
-    cells: CellResolver,
+    cells: Arc<Mutex<CellResolver>>,
     ignore_specs: StdBuckHashMap<CellName, IgnoreSet>,
     empty_on_fresh_instance: bool,
     report_global_rev: bool,
@@ -118,14 +116,12 @@ impl WatchmanQueryProcessor {
             return Ok(());
         }

-        let cell_path = self.cells.get_cell_path(path);
+        let cells = self.cells.lock().unwrap();
+        let cell_path = cells.get_cell_path(path);

         let ignore = self
             .ignore_specs
             .get(&cell_path.cell())
-            // This shouldn't ever really happen. However, because of the bugs caused by just
-            // storing the `CellResolver` in the watcher permanently, sometimes it can, so we just
-            // default to not ignoring the file in that case
             .is_some_and(|ignore| ignore.is_match(cell_path.path()));

         info!("Watchman: {:?} (ignore = {})", ev, ignore);
@@ -345,6 +341,8 @@ impl SyncableQueryProcessor for WatchmanQueryProcessor {
 pub(crate) struct WatchmanFileWatcher {
     #[allocative(skip)]
     query: SyncableQuery<buck2_data::FileWatcherStats, DiceTransactionUpdater>,
+    #[allocative(skip)]
+    cells: Arc<Mutex<CellResolver>>,
 }

 /// The watchman query is constructed once on daemon startup. It is an unfiltered watchman query
@@ -393,6 +391,8 @@ impl WatchmanFileWatcher {
         let dice_clear_on_mergebase_change =
             crate::file_watcher::dice_clear_on_mergebase_change(root_config)?;

+        let cells = Arc::new(Mutex::new(cells));
+
         let query = SyncableQuery::new(
             Connector::new(),
             project_root,
@@ -402,7 +402,7 @@ impl WatchmanFileWatcher {
                 Expr::FileType(FileType::Symlink),
             ]),
             Box::new(WatchmanQueryProcessor {
-                cells,
+                cells: cells.clone(),
                 ignore_specs,
                 empty_on_fresh_instance,
                 report_global_rev,
@@ -415,7 +415,7 @@ impl WatchmanFileWatcher {
             dice_clear_on_mergebase_change,
         )?;

-        Ok(Self { query })
+        Ok(Self { query, cells })
     }
 }

@@ -424,7 +424,10 @@ impl FileWatcher for WatchmanFileWatcher {
     async fn sync(
         &self,
         dice: DiceTransactionUpdater,
+        cells: CellResolver,
     ) -> buck2_error::Result<(DiceTransactionUpdater, Mergebase)> {
+        *self.cells.lock().unwrap() = cells;
+
         span_async(
             buck2_data::FileWatcherStart {
                 provider: buck2_data::FileWatcherProvider::Watchman as i32,
diff --git a/app/buck2_server/src/ctx.rs b/app/buck2_server/src/ctx.rs
index c71143e7a3..26df2a8379 100644
--- a/app/buck2_server/src/ctx.rs
+++ b/app/buck2_server/src/ctx.rs
@@ -633,7 +633,7 @@ impl DiceUpdater for DiceCommandUpdater<'_, '_> {

         setup_interpreter(
             &mut ctx,
-            cell_resolver,
+            cell_resolver.dupe(),
             configuror,
             cells_and_configs.external_data,
             profiler_instrumentation_override.clone(),
@@ -647,7 +647,7 @@ impl DiceUpdater for DiceCommandUpdater<'_, '_> {
             .base_context
             .daemon
             .file_watcher
-            .sync(ctx)
+            .sync(ctx, cell_resolver)
             .await?;
         early_timings.end_known_span();

diff --git a/tests/core/cells/test_file_watcher_resolution.py b/tests/core/cells/test_file_watcher_resolution.py
index bace6acf45..3192606927 100644
--- a/tests/core/cells/test_file_watcher_resolution.py
+++ b/tests/core/cells/test_file_watcher_resolution.py
@@ -10,11 +10,12 @@


 from buck2.tests.e2e_util.api.buck import Buck
+from buck2.tests.e2e_util.asserts import expect_failure
 from buck2.tests.e2e_util.buck_workspace import buck_test


 @buck_test()
-async def test_changing_cell_location_bug(buck: Buck) -> None:
+async def test_changing_cell_location(buck: Buck) -> None:
     await buck.targets("foo//:", "bar//:")

     # Switch the location of the 2 cells
@@ -27,13 +28,4 @@ async def test_changing_cell_location_bug(buck: Buck) -> None:

     (buck.cwd / "foo" / "TARGETS.fixture").write_text("fail('error')")

-    # FIXME(JakobDegen): The change to the `TARGETS.fixture` file does not get picked up by buck.
-    # The cause is that the file watcher always invalidates injected keys computed from `CellPath`s,
-    # but the `CellResolver` that it uses to map `ProjectRelativePath`s to `CellPath`s is computed
-    # once at daemon startup and never updated. So concretely, the file update above results in the
-    # cell path `bar//TARGETS.fixture` being invalidated, which means the targets in `foo//:` are
-    # never recomputed.
-    #
-    # This is just one example, there's a thousand other ways that you can change the `CellResolver`
-    # to create similar bugs.
-    await buck.targets("foo//:", "bar//:")
+    await expect_failure(buck.targets("foo//:", "bar//:"))
