# SPDX-License-Identifier: MPL-2.0

load("//rules:crate.bzl", "crate")
load("//rules:include.bzl", "include")

crate(
    name="kernel",
    emit=["obj"],
    root="lib.rs",
    srcs=glob(["**/*.rs"]),
)

include(
    name="ld-include",
    files=glob(["**/*.ld"]),
)
